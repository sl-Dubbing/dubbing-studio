services:
  - type: web
    name: sl-dubbing-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT  # ✅ مهم جداً
    envVars:
      - key: PORT
        value: 5000
